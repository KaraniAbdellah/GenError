// Search in UserData and Get Session to Hold on Context Just By Id and Return Session

import { UserType } from "@/types/UserTypes";
function getSessionToHold(userData: UserType, id: string) {
  return 0;
}
