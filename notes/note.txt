the idea is to type an error and we are going to generate a message for this error to display it to user.


nich: all developers
features:
    limit prompting in 50 if it is big --> login
    generate message
    offre toast code and UI to alter

tools:
    redux
    typescript
    tanstack query
    shadcn
    ORM (prisma, ...)

// Use ready template from shadcn